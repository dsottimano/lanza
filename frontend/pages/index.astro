---
// The site root — the default locale's home. Rendered as CMS content (the page
// with slug "home" in the default locale) through the shared HomeRoute, exactly
// like the localized mirror at /<locale>/. Edit it in the CMS: Pages → "Home",
// pick a Template, fill its slots.
import HomeRoute from "../components/HomeRoute.astro";
import { DEFAULT_LOCALE } from "../lib/i18n";
---

<HomeRoute locale={DEFAULT_LOCALE} />
