# Context handoff: 2026-09-07

Start with [todos.md](todos.md), [product-vision.md](product-vision.md) and
[thoughts-v1.md](thoughts-v1.md), then [publishing.md](publishing.md) for the latest
incident and repair. Earlier checkpoints below are historical; the following update
supersedes their commit, push and validation status.

## Latest update: schema, blocks and publishing

- Product guides, Architecture and Blog are CMS Pages in both languages. Schema,
  template-slot and public-route checks now gate the build. `AGENTS.md` is mandatory.
- Separate page blocks are retired; no repository entries used them. Legacy tenant
  rendering is preserved and verified. Rich-text reuse is named Saved snippets.
- `1bfafeb` pushed the accumulated work to main, but omitted pending CMS changes on
  staging. That caused the owner's homepage publish conflict. The agent must check
  both branches before changing content or publishing; a clean main is insufficient.
- The owner's latest text is preserved in the new homepage fields. The legal page's
  empty Manifesto selection was removed so its body renders. Both branches received
  reconciled commit `7239901` via an atomic push.
- CMS publishing now reviews fixed SHAs, detects stale reviews and divergence,
  updates drafts without publishing, and fast-forwards production to the reviewed
  commit. Inline conflict recovery replaces the raw merge-conflict modal.
- The local pre-push guard is installed. It is not server-side protection. MCP's
  separate publishing client still needs the same reviewed-commit behavior.
- Latest full suite: 347 Node tests and 409 CMS tests passed. Type checks, CMS/public
  builds, and site validation passed. Exact homepage copy and legal body were checked
  in built HTML. Earlier packed-tenant/legacy-block checks also passed.
- Browser verification of the deployed UI remains open. Cloudflare deployment was
  in progress when first checked after the push. No npm release, credential change,
  or real private-note write was performed.

## User decisions, in order of precedence

The product is a system for capturing, developing, owning and eventually publishing
human thought. AI labor should make more room for human thinking, not generate more
content. Original notes, sources and evolution matter; publishing is optional.

The human uses ChatGPT or Claude, ideally voice mode, to think aloud and have the
assistant organize/save thoughts. Lanza is the durable store and human review surface.
An assistant-proposed CMS capture form/voice recorder was the wrong starting point.

D1/R2 free tiers were investigated, but the user then chose a simpler v1: notes in
the person's private Lanza GitHub repo. Do not reopen the private-repo-versus-device
question or start provisioning a database. A separate repo is not required. The
existing public product repo is the exception, not a suitable private notebook.

Crawler visibility is desired later. Read-only Cloudflare analytics permissions
were discussed; raw HTTP request logs are plan-dependent. No OAuth app changes were
verified. Existing onboarding credentials remain temporary; no broker token store.

## Implemented locally in this conversation

### Sidebar and local identity

- `admin/src/ui/Sidebar.vue`, `SidebarIcon.vue`, `Sidebar.test.ts`: consistent icons,
  softer selection, clearer grouping and publishing controls. Fixed multiple active
  rows, collapsible section behavior and hidden keyboard targets.
- Design is now Appearance; Structure is Site settings; Site is Administration.
  Owner-only sections remain gated. Review changes and Publish site are separate.
- `admin/dev/github-identity.ts`, `admin/vite.config.ts`, and
  `admin/src/backend/github-identity.test.ts`: Vite's `/admin/api/gh/user` was forwarding
  GitHub's raw user response without a role. It now checks repository permissions
  with the shared role mapping and returns the production identity shape.
- Verified local endpoint changed from `role:null` to `role:"owner"` for dsottimano.
  This was the cause of the user's missing design/settings sections, not collapse state.

### Content types

- `ContentTypesView.vue`, `content-types/EditingExperience.vue` and tests: explicit
  editing-experience cards; expandable fields; camelCase/underscore field validation;
  named validation errors; preserve additional collection metadata during save.
- Existing behavior remains: `body:'none'` opens the field form; rich collections
  open the writing editor. Pages with a preset use template fields/live preview
  when that template does not use a rich body. No new editor-mode schema was added.
- Removal exists at Site settings → Content types → choose type → Advanced settings
  → Remove content type, then Save. Confirmation removes the CMS declaration, not
  content files. The user found this too hidden; visibility has not been improved.

### Header/footer and languages

- `HeaderFooterView.vue`, `MenuEditor.vue` and tests: navigation first, recognizable
  area picker, larger sticky preview and a single collapsed advanced template editor.
  Removed misleading draggable HTML fragments. Ordinary menu edits preserve templates.
- Preview loads the site name and marks the selected locale. Desktop-link preview
  is explicitly labeled; it is not a verified responsive preview of all device menus.
- Added load-failure handling that blocks saves; test covers correct footer binding.
- `App.vue`: settings locale query drives the header/footer language selector and
  remount. Existing router guard protects unsaved changes. Menus save per locale;
  template layout is shared. Translation is manual or agent-assisted.

### Public product site

- `content/pages/{en,es}/home.md` and `data/seo.{en,es}.json`: new promise and
  human-thinking philosophy, with shipped publishing versus planned features distinguished.
- This copy was written BEFORE the final conversation-first/GitHub-only clarification.
  It still needs alignment; do not claim that final messaging is complete.
- Existing screenshots show the writing CMS, not a working notebook. Do not relabel
  them as proof of thought capture or long-term assistant memory.

## Earlier verification checkpoints (superseded above)

- Sidebar/identity tests: 23 passed at that stage.
- Content-type tests: 3 passed; admin build passed.
- Header/footer and section tests: 14 passed after redesign; header/footer alone
  reached 7 passing tests after language support. Type checking passed afterward.
- Public site build: 15 pages built successfully after homepage changes.
- `npm run check:site`: two errors for `menuLabel` and `primaryNavigationLabel` in
  `templates/parts/header.html`, despite successful exit status. They were not fixed.
- No full-suite run or browser visual verification was performed for this complete
  set of September 7 edits. No packed-package tenant build was performed.

These are observed checkpoints, not an instruction to sum counts into a claimed
full-suite total. Recheck after implementation changes and before release.

## What is not built

No note schema, storage API, note tools, Thoughts screen, private-repository guard,
note search, connections, provenance implementation or note-to-article promotion.
Voice-mode tool compatibility has not been established. No database, bucket or
private repo was created. Git branch/Discard behavior for durable notes is unresolved.

The next useful work is the private-tenant conversation → saved note → retrieval
round trip, with file/privacy/save semantics resolved before broad UI work.
