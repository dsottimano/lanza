{
  "name": "site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build:studio": "npm --prefix cms ci && npm --prefix cms run build",
    "build": "npm run build:studio && node scripts/gen-redirects.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "7.0.3",
    "isomorphic-dompurify": "3.18.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "typescript": "6.0.3"
  }
}
