// The site owner's GitHub login — the ADMIN_LOGIN allowlist for /admin (comma list
// for extra editors). The onboarding broker OVERWRITES this file at repo creation
// with the owner's login; the template ships it empty. An ADMIN_LOGIN env var
// overrides it (the dogfood site uses that). Edit via the broker, not by hand.
export const ADMIN_LOGIN = "";
