# Astro static site + Studio CMS + Telegram bot (Cloudflare)

Pure-static Astro at the repo root, edited through **Studio** — a self-owned
Vue 3 + TipTap CMS (`cms/`) served at `/admin` — with a Telegram Worker for
drafting on the go. Everything runs on Cloudflare's free tier. Content lives in
git; the production build drops `draft: true` entries. Post/page **bodies are
HTML** (Studio is the source of truth); Astro renders them with `set:html`,
sanitized at build time (`src/lib/sanitize.ts`).

No Cloudflare adapter yet — add `@astrojs/cloudflare` + per-route
`prerender = false` only when the real-estate listings need on-demand rendering
(those will move to D1).

```
.                 Astro project (root)
├── src/          pages, content collections, data (seo.json, redirects.json)
├── public/       static assets; /admin = Studio build output (gitignored)
├── cms/          Studio CMS source (Vue 3 + Vite + TS + TipTap)
├── scripts/      gen-redirects.mjs (redirects.json → public/_redirects)
└── bot/          Telegram → draft-post Cloudflare Worker (own README)
```

## Local dev

```bash
npm install
npm run dev            # Astro site; drafts ARE visible in dev
npm --prefix cms run dev   # Studio with HMR at http://localhost:5173/admin/index.html
npm run build         # builds Studio → public/admin, then gen-redirects → astro build
npm run check         # astro check (type-check)
```

Studio's build output (`public/admin/`) is gitignored and regenerated by the
build, so a bare `npm run dev` won't serve `/admin` until you've built it
(`npm run build:studio`) or just run the Studio dev server above.

## Editing content — Studio at `/admin`

- Deploy, then visit `https://<your-site>/admin/`.
- Sign in with a GitHub **fine-grained PAT** (this repo, **Contents: read &
  write**) via **"Sign in with Token"**. The token is kept in `localStorage`; no
  auth Worker needed for a solo/technical editor.
- **Drafts:** new entries default to `draft: true` and stay off the live site
  until you flip *Published* and save.
- **Collections, fields, and settings** are all defined in one place —
  `cms/src/schema.ts` (the content model; there is no `config.yml`).
- **Media:** images upload straight to `public/images/uploads` and are served as
  static assets.
- **SEO:** per-entry in the settings drawer; site-wide defaults under
  *Settings → SEO defaults* (`src/data/seo.json`).
- **Redirects:** *Settings → Redirects* (`src/data/redirects.json`), compiled to
  Cloudflare's native `public/_redirects` at build time by `scripts/gen-redirects.mjs`.

See `cms/PROGRESS.md` for the build history and architecture notes.

## Deploy to Cloudflare Pages

Connect the repo in the Pages dashboard with:

| Setting | Value |
|---|---|
| Root directory | `/` |
| Build command | `npm run build` |
| Output directory | `dist` |

`npm run build` installs Studio's dependencies (`cms/`), builds the CMS into
`public/admin`, then runs the Astro build — so `/admin` ships with every deploy.
Static page views are unmetered (free tier). Update `site:` in `astro.config.mjs`
to your real domain, and the repo coordinates in `cms/src/backend/config.ts` if
you fork.
