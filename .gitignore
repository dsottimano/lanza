# Dependencies & build output
node_modules/
dist/
.astro/

# Studio CMS build output — the admin app, regenerated from cms/ by the deploy
# build (`npm run build:studio`). Not committed; same model the old /studio used.
public/admin/

# EmDash runtime (regenerated by `npx emdash dev` — seed re-applies)
data.db
uploads/

# Secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Tooling working dir
.superpowers/
